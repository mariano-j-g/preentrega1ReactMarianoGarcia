const Navbar = () => {
  return (
    <nav>
      <h1>Ecommerce</h1>
      <section>
        <button>celulares</button>
        <button>Tablets</button>
        <button>NoteBooks</button>
      </section>
    </nav>
  );
};

export default NavBar;
